workflow:
  build:
    strategy:
      # parallel: 5
      
      # fail-fast: false
      matrix:
        location: [ 'C:\', 'D:\', 'C:\Windows' ]

    runs-on:
      pool: default

    actions:
    # - run: set Job_Position
    #   name: printenv_name
    #   id: printenv_id
    #   if: succeeded()
    #   env:
    #    Job_Position: ${{ strategy['job-index'] }}
    #   working-directory: c:\
   
    - run: dir
      working-directory: ${{ strategy.matrix.location }}

    # - uses: bryanmacfarlane/node-config@master
    #   with:
    #     version: ${{ strategy.matrix.nodeVersion }}

    # - uses: 'docker://ubuntu:16.04'
    #   if: succeeded()
    #   with:
    #     entryPoint: /bin/bash
    #     args: -c printenv
    #   env:
    #     foo: bar
    # - uses: TingluoHuang/npm@master
    #   name: npm_version
    #   with:
    #     args: version
    # - uses: ./npm
    #   name: npm_version_local
    #   with:
    #     args: version
    # - uses: actions/aws/cli@master
    #   id: cli_id
    #   with:
    #     args: --output text --color off help
    
    
